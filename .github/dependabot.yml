version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: daily
    cooldown:
      default-days: 7
    commit-message:
      prefix: ci(github-actions)
    groups:
      github-actions:
        update-types: [minor, patch]
