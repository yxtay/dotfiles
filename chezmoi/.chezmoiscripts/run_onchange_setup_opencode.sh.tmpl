#!/usr/bin/env bash
set -euo pipefail

if ! command -v npm >/dev/null; then
  exit
fi

if ! command -v opencode >/dev/null; then
  npm install --global opencode-ai
fi

npm exec --yes --allow-git=all -- github:JuliusBrussee/caveman -- --only opencode
